\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{amsmath}

\title{Projet Math \And Signal}
\author{Lucas DAGON, Nolann WICKERS}
\date{Janvier 2026}

\begin{document}
\maketitle
\section*{Description du projet}

\subsection*{Introduction}
Interpréter les données situés dans le fichier \texttt{data/strasbourg\_entzheim.csv} pour pouvoir lancer des analyses descriptives des données de manière scientifique.

\subsection*{Organisation générale du projet}
Les fichiers de ce dépôt se répartissent en grandes catégories :
\begin{itemize}
	\item Données brutes (CSV) : fichiers originaux temporels d'observation météorologique.
	\item Résumés statistiques (fichiers \texttt{summary\_*.csv}) : sorties de scripts de synthèse.
	\item Scripts et modules : lecteur CSV, filtrage de signal, et fonctions de synthèse.
\end{itemize}

\paragraph{Types et unités}
Avant toute interprétation, on doit vérifier les métadonnées pour confirmer les unités:
\begin{itemize}
	\item Températures (\texttt{tavg, tmin, tmax}) : degrés (°C).
	\item Précipitations (\texttt{prcp}) : millimètres (mm) par période d'observation.
	\item Chute de neige (\texttt{snow}) :  millimètres (mm).
	\item Vent (\texttt{wspd}, \texttt{wpgt}) : killomètre par heure (km/h).
	\item Pression (\texttt{pres}) : hectoPascal (hPa) ou millibar.
	\item Ensoleillement (\texttt{tsun}) : minutes d'ensoleillement.
\end{itemize}

\paragraph{Valeurs manquantes et qualité}
Les colonnes peuvent contenir des valeurs manquantes (chaînes vides, \texttt{NA}, ou \texttt{NaN}).
Avant l'analyse, normaliser les valeurs manquantes en \texttt{NaN} et produire un bilan par variable : comptage, pourcentage.

\subsection*{Fichiers de synthèse}
Exemples : \texttt{summary\_tavg.csv}, \texttt{summary\_tsun.csv}, etc. Ces fichiers contiennent des métriques
décrivant la distribution d'une variable (moyenne, écart-type, quantiles, nombre de valeurs manquantes, etc.), ces fichiers sont crées automatiquement après execution du script.

\subsection*{Recommandations d'interprétation spécifiques par variable}
\begin{itemize}
	\item Températures (tavg,tmin,tmax) : vérifier cohérence physique (tmin, tavg, tmax), rechercher discontinuités journalières.
	\item Précipitations (\texttt{prcp}) : distribution fortement asymétrique (beaucoup de zéros) — ne pas utiliser la moyenne seule pour décrire la variabilité; employer quantiles et fréquence d'occurrence (nombre de jours $>$ seuil).
	\item Chute de neige (\texttt{snow}) : traiter comme précipitation mais vérifier unité et seuils de mesure; forte présence de zéros attendue.
	\item Vent (\texttt{wspd,wpgt}) : distinguer vitesse moyenne et rafales; pour la sécurité, analyser les maxima à court terme et la distribution des rafales.
	\item Pression (\texttt{pres}) : regarder dérives lentes et oscillations synoptiques; utile pour corrélation avec autres variables.
	\item Ensoleillement (\texttt{tsun}) : souvent mesuré en minutes par jour; vérifier la granularité temporelle et convertir en heures si nécessaire.
\end{itemize}

\newpage

\section*{1. Signification des métriques descriptives}
Contexte : résumé pour une colonne numérique (ex. \texttt{tsun}).

\begin{description}
  \item[Count] nombre d'observations numériques valides.
  \item[Missing] nombre de valeurs manquantes / non numériques.
  \item[Mean] moyenne arithmétique (sensible aux outliers).
  \item[Std] écart-type (ddof=1) : dispersion autour de la moyenne.
  \item[Min / Max] valeurs extrêmes observées (vérifier plausibilité).
  \item[Quantiles] $q_{1},q_{5},q_{25},q_{50}(=\text{médiane}),q_{75},q_{95},q_{99}$ : position relative des valeurs.
  \item[Skew] asymétrie : $>0$ queue droite (valeurs élevées rares), $<0$ queue gauche.
  \item[Kurtosis] aplatissement : $>0$ queues lourdes, $<0$ distribution plus plate que la normale.
  \item[Outliers (IQR)] règle IQR : seuils $[Q1-1.5\cdot IQR,\,Q3+1.5\cdot IQR]$ ; compte des valeurs hors intervalle.
  \item[PSD peak freq] fréquence dominante du spectre (Welch) ; période associée $\approx 1/f_{\text{peak}}$ (en échantillons).
\end{description}

Conseils pratiques :
\begin{itemize}
  \item Si beaucoup de \texttt{Missing} : imputation ou filtrage avant analyse.
  \item Si \texttt{mean} $\gg$ \texttt{median} et \texttt{skew}$>0$ : présence d'outliers à droite.
  \item Utiliser PSD et autocorr pour détecter périodicités vs bruit.
  \item Combiner visualisations (histogramme, boxplot, autocorr, PSD) pour validation qualitative.
\end{itemize}

\newpage

\section*{4. Estimation de la proportion de bruit}
But : estimer la part de variance due au bruit dans une série temporelle.

\begin{itemize}
  \item Lissage : $\tilde{x}$ obtenu par Savitzky–Golay (ou moyenne mobile).
  \item Résidu (bruit estimé) : $r = x - \tilde{x}$.
  \item Variances :
    \[
      \sigma_{\text{bruit}}^2 = \operatorname{Var}(r),\qquad
      \sigma_{\text{tot}}^2 = \operatorname{Var}(x).
    \]
  \item Proportion de bruit :
    \[
      p_{\text{bruit}} = \frac{\sigma_{\text{bruit}}^2}{\sigma_{\text{tot}}^2}\in[0,1].
    \]
  \item SNR (en dB) :
    \[
      \text{SNR}_{\text{dB}} = 10\log_{10}\!\left(\frac{\ma0,\sigma_{\text{tot}}^2-\sigma_{\text{bruit}}^2)}{\sigma_{\text{bruit}}^2}\right).
    \]
  \item Sorties utiles : courbe lissée $\tilde{x}$, résidu $r$, $p_{\text{bruit}}$, $\text{SNR}_{\text{dB}}$.
\end{itemize}

Interprétation qualitative rapide :
\begin{itemize}
  \item $p_{\text{bruit}}\to 0$ : variance majoritairement due au signal (peu de bruit).
  \item $p_{\text{bruit}}\to 1$ : variance majoritairement due au bruit.
  \item Règles SNR (empiriques) : $>20\,$dB propre, $10$–$20\,$dB acceptable, $0$–$10\,$dB bruité, $\le 0\,$dB bruit dominant.
  \item Vérifications : tracer $x,\tilde{x},r$ ; histogramme et autocorr($r$) (bruit blanc attendu) ; PSD pour séparer basses fréquences (signal) et hautes (bruit).
  \item Limites : suppose bruit additif et que le lissage capture le signal utile ; paramètres de lissage influent fortement.
\end{itemize}



\end{document}
